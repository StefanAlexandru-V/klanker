services:
  searxng:
    image: searxng/searxng:latest
    container_name: klanker-searxng
    ports:
      - "8888:8080"
    environment:
      - SEARXNG_BASE_URL=http://localhost:8888/
    volumes:
      - ./searxng:/etc/searxng
    restart: unless-stopped
