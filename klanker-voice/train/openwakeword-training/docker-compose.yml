services:
  kokoro:
    image: ghcr.io/remsky/kokoro-fastapi-cpu:latest
    ports:
      - "8880:8880"

  trainer:
    build: .
    volumes:
      - ./data:/app/data
      - ./my_real_samples:/app/my_real_samples
      - ./my_custom_model:/app/my_custom_model
    environment:
      - KOKORO_URL=http://kokoro:8880
    depends_on:
      - kokoro
