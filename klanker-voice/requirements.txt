# Klanker Voice Assistant
# Install on Windows: pip install -r requirements.txt

# GUI
PyQt6>=6.6.0

# Wake word detection (local, no API keys)
openwakeword>=0.6.0

# Speech-to-text (Whisper via CTranslate2 — pre-built wheels, CPU mode)
# Note: faster-whisper uses CPU on AMD GPUs (no CUDA). Still fast enough for short utterances.
faster-whisper>=1.0.0

# Audio capture
sounddevice>=0.4.6
numpy>=1.24.0

# HTTP client for LM Studio API
httpx>=0.27.0

# Database
# (uses Python built-in sqlite3 — no extra dependency)

# Testing
pytest>=8.0.0
pytest-qt>=4.4.0
